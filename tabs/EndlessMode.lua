EndlessMode = class()

function EndlessMode:init(x)
    -- you can accept and set parameters here
    self.x = x
end

function EndlessMode:draw()
    -- Codea does not automatically call this method
end

function EndlessMode:touched(touch)
    -- Codea does not automatically call this method
end
